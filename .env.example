# Database (required for app and scripts)
# Local Supabase: postgresql://postgres:postgres@127.0.0.1:54322/postgres (after bun run supabase:start)
# Remote: Supabase Project Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Gemini AI Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Model name for recipe details (e.g. gemini-2.5-flash-lite, gemini-2.0-flash)
GEMINI_MODEL=gemini-2.5-flash-lite

# Main page password protection
# ACCESS_PASSWORD
ACCESS_PASSWORD=some-password
# This is a random secret generated by the command:
# openssl rand -base64 32
ACCESS_COOKIE_SECRET=some-secret-string


node -e 'const { randomBytes, scryptSync } = require("node:crypto"); const p="SysGears"; const salt=randomBytes(16); const hash=scryptSync(p, salt, 64); console.log("scrypt$" + salt.toString("hex") + "$" + hash.toString("hex"));'

scrypt\$5149f04a17174dd7a1c0ba4603e12cf3\$3a35669ebe12acf34188eea1fe648ef9f142427435969db356612a3373e385888cfd954a85d3522bbc4390af8625dd39feea776fa765f25634b3cabc2e206d94